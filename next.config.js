/** @type {import('next').NextConfig} */
const repo = "portfolio-react01";

module.exports = {
  reactStrictMode: true,

  output: "export",
  trailingSlash: true,

  images: { unoptimized: true },
};
